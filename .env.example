# WebSocket Configuration
# Note: Only enable after WebSocket server is running
# Development: ws://localhost:8080 or ws://localhost:3001
# Production: wss://api.yourapp.com/ws
VITE_WEBSOCKET_URL=ws://localhost:8080

# API Configuration
# Development: http://localhost:3000
# Production: https://api.yourapp.com
VITE_API_URL=http://localhost:3000

# Feature Flags
# VITE_ENABLE_WEBSOCKET: Set to 'true' only when WebSocket server is running
# VITE_ENABLE_LOCAL_SIMULATION: Fallback when WebSocket is unavailable
VITE_ENABLE_WEBSOCKET=false
VITE_ENABLE_LOCAL_SIMULATION=true

# Application Environment
VITE_ENVIRONMENT=development
